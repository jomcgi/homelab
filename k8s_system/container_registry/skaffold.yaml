apiVersion: skaffold/v4beta11
kind: Config
metadata:
  name: container-registry

manifests:
  kustomize:
    paths:
    - ./k8s_system/container_registry
deploy:
  kubectl:
    defaultNamespace: container-registry

        
