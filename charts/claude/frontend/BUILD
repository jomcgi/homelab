# Pre-built frontend assets
# Build locally with: cd charts/claude/frontend && pnpm install && pnpm exec vite build
filegroup(
    name = "dist",
    srcs = glob(["dist/**/*"]),
    visibility = ["//charts/claude/image:__pkg__"],
)
