{{- if .Values.sandboxes.namespace.create }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.kubernetes.sandboxNamespace }}
  labels:
    {{- include "openhands.labels" . | nindent 4 }}
  annotations:
    linkerd.io/inject: "disabled"
{{- end }}
