# Default values for argocd wrapper chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# All values below are passed to the argo-cd subchart
argo-cd:
  # Add your ArgoCD configuration here
  # Example:
  # server:
  #   extraArgs:
  #     - --insecure