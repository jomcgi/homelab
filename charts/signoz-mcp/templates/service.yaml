apiVersion: v1
kind: Service
metadata:
  name: {{ include "signoz-mcp.fullname" . }}
  labels:
    {{- include "signoz-mcp.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.port }}
      targetPort: mcp
      protocol: TCP
      name: mcp
  selector:
    {{- include "signoz-mcp.selectorLabels" . | nindent 4 }}
