apiVersion: apps/v1
kind: Deployment
metadata:
  name: horoscope-bot
  labels:
    app: horoscope-bot
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: horoscope-bot
  template:
    metadata:
      labels:
        app: horoscope-bot
    spec:
      containers:
      - name: horoscope-bot
        image: horoscope-bot
        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "250m"
            memory: "256Mi"
