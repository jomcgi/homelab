apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-bot
  labels:
    app: hello-bot
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: hello-bot
  template:
    metadata:
      labels:
        app: hello-bot
    spec:
      containers:
      - name: hello-bot
        image: hello-bot
        resources:
          limits:
            cpu: "500m"
            memory: "512Mi"
          requests:
            cpu: "250m"
            memory: "256Mi"
