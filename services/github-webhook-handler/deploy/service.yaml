apiVersion: v1
kind: Service
metadata:
  name: webhook-handler
  labels:
    app: webhook-handler
spec:
  type: NodePort
  ports:
  - port: 9090
    targetPort: 9090
    nodePort: 30320
    protocol: TCP
    name: github-webook
  selector:
    app: webhook-handler