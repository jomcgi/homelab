# from discord.ext import tasks

# @tasks.loop(hours=24)
# async def change_role(self):
#     members = ? #a way to get the members you want.
#     role = self.bot #replace guildID and roleID (or if it's for multiple servers then use a for loop for an iterable of whatever their IDs are.
#     for member in members:
#         await member.add_roles([role])

# change_role.start()


