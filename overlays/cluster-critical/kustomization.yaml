apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ./agent-sandbox
  - ./argocd
  - ./argocd-image-updater
  - ./cert-manager # Must be before linkerd
  - ./coredns
  - ./kyverno
  - ./opentelemetry-operator
  - ./envoy # Installs Gateway API CRDs; must be before linkerd
  - ./linkerd
  - ./longhorn
  - ./nvidia-gpu-operator
  - ./signoz
  - ./signoz-dashboard-sidecar # Must be after signoz
