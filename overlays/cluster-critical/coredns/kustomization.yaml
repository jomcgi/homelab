apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kube-system

resources:
  - application.yaml

patches:
  - path: configmap-patch.yaml
    target:
      kind: ConfigMap
      name: coredns
