# Cluster-specific values for envoy-gateway
# These override the defaults in charts/envoy-gateway/values.yaml

gateway-helm:
  # Add cluster-specific configuration here
  config:
    envoyGateway:
      gateway:
        controllerName: gateway.envoyproxy.io/gatewayclass-controller
