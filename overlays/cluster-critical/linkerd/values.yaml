# Cluster-specific Linkerd configuration overrides
# This file contains environment-specific values for the homelab cluster

# No cluster-specific overrides needed yet
# Certificate management is handled by cert-manager (see charts/linkerd/values.yaml)
# If you need to override proxy resources or other settings, add them here:
#
# linkerd-control-plane:
#   proxy:
#     resources:
#       cpu:
#         request: 50m  # Lower for homelab
