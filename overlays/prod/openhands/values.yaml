# Image pull secret for GHCR
imagePullSecret:
  enabled: true

# 1Password secret paths
secrets:
  claudeSdkToken:
    itemPath: "vaults/k8s-homelab/items/claude-sdk-token"
  openhandsSecrets:
    itemPath: "vaults/k8s-homelab/items/openhands-secrets"

# Kubernetes runtime
kubernetes:
  pvcStorageClass: "longhorn"

# Workspace persistence
app:
  persistence:
    storageClass: "longhorn"
