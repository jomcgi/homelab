apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grimoire
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/jomcgi/homelab.git
    path: projects/grimoire/deploy
    targetRevision: HEAD
    helm:
      releaseName: grimoire
      valueFiles:
        - values.yaml
        - ../../../../overlays/dev/grimoire/values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: grimoire
  # Ignore fields injected by OTel operator and Kyverno policies
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jqPathExpressions:
        - .spec.template.metadata.annotations."otel.injected-by"
        - .spec.template.spec.containers[].env
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
