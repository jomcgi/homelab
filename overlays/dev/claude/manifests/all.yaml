---
# Source: claude/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: claude
  labels:
    helm.sh/chart: claude-0.1.0
    app.kubernetes.io/name: claude
    app.kubernetes.io/instance: claude
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/managed-by: Helm
automountServiceAccountToken: true
---
# Source: claude/templates/nginx-configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: claude-nginx
  labels:
    helm.sh/chart: claude-0.1.0
    app.kubernetes.io/name: claude
    app.kubernetes.io/instance: claude
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/managed-by: Helm
data:
  nginx.conf: |
    user nginx nginx;
    worker_processes auto;
    error_log /var/log/nginx/error.log warn;
    pid /tmp/nginx.pid;

    events {
        worker_connections 1024;
    }

    http {
        include /etc/nginx/mime.types;
        default_type application/octet-stream;

        access_log /dev/stdout;
        error_log /dev/stderr warn;

        sendfile on;
        tcp_nopush on;
        tcp_nodelay on;
        keepalive_timeout 65;

        # Temp paths for non-root
        client_body_temp_path /tmp/client_body;
        proxy_temp_path /tmp/proxy;
        fastcgi_temp_path /tmp/fastcgi;
        uwsgi_temp_path /tmp/uwsgi;
        scgi_temp_path /tmp/scgi;

        server {
            listen 8080;
            server_name _;

            # Proxy all requests to backend
            location / {
                proxy_pass http://127.0.0.1:3000;
                proxy_http_version 1.1;
                proxy_set_header Host $host;
                proxy_set_header X-Real-IP $remote_addr;
                proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
                proxy_set_header X-Forwarded-Proto $scheme;

                # WebSocket support
                proxy_set_header Upgrade $http_upgrade;
                proxy_set_header Connection "upgrade";

                # Long timeouts for WebSocket connections (7 days)
                proxy_connect_timeout 7d;
                proxy_send_timeout 7d;
                proxy_read_timeout 7d;
            }

            # Health check
            location /health {
                access_log off;
                return 200 "healthy\n";
                add_header Content-Type text/plain;
            }

            # Dev server preview routing
            # Proxies /preview/:port/* to localhost:port/*
            # Example: /preview/5173/index.html → localhost:5173/index.html
            location ~ ^/preview/(\d+)/(.*)$ {
                set $preview_port $1;
                set $preview_path $2;

                # Proxy to the dev server running in the same pod
                proxy_pass http://127.0.0.1:$preview_port/$preview_path$is_args$args;
                proxy_http_version 1.1;

                # WebSocket support for HMR (Hot Module Reload)
                proxy_set_header Upgrade $http_upgrade;
                proxy_set_header Connection "upgrade";

                # Standard proxy headers
                proxy_set_header Host $host;
                proxy_set_header X-Real-IP $remote_addr;
                proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
                proxy_set_header X-Forwarded-Proto $scheme;

                # Reasonable timeouts for dev servers
                proxy_connect_timeout 60s;
                proxy_send_timeout 60s;
                proxy_read_timeout 60s;
            }

            # Handle /preview/:port without trailing slash
            location ~ ^/preview/(\d+)$ {
                return 301 $scheme://$host/preview/$1/;
            }
        }
    }
---
# Source: claude/templates/pvc.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: claude-home
  labels:
    helm.sh/chart: claude-0.1.0
    app.kubernetes.io/name: claude
    app.kubernetes.io/instance: claude
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/managed-by: Helm
  annotations:
    helm.sh/resource-policy: keep
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 200Gi
---
# Source: claude/templates/clusterrole.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: claude
  labels:
    helm.sh/chart: claude-0.1.0
    app.kubernetes.io/name: claude
    app.kubernetes.io/instance: claude
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/managed-by: Helm
rules:
  # Read-only access to all resources (safe for observability)
  - apiGroups: ["*"]
    resources: ["*"]
    verbs: ["get", "list", "watch"]
  # Allow port-forwarding for debugging
  - apiGroups: [""]
    resources: ["pods/portforward"]
    verbs: ["create"]
  # Allow exec into pods for debugging
  - apiGroups: [""]
    resources: ["pods/exec"]
    verbs: ["create"]
  # Allow logs access
  - apiGroups: [""]
    resources: ["pods/log"]
    verbs: ["get"]
  # Allow creating jobs from cronjobs
  - apiGroups: ["batch"]
    resources: ["jobs"]
    verbs: ["create"]
---
# Source: claude/templates/clusterrolebinding.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: claude
  labels:
    helm.sh/chart: claude-0.1.0
    app.kubernetes.io/name: claude
    app.kubernetes.io/instance: claude
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/managed-by: Helm
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: claude
subjects:
  - kind: ServiceAccount
    name: claude
    namespace: claude
---
# Source: claude/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: claude
  labels:
    helm.sh/chart: claude-0.1.0
    app.kubernetes.io/name: claude
    app.kubernetes.io/instance: claude
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/managed-by: Helm
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 8080  # nginx proxy
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: claude
    app.kubernetes.io/instance: claude
---
# Source: claude/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: claude
  labels:
    helm.sh/chart: claude-0.1.0
    app.kubernetes.io/name: claude
    app.kubernetes.io/instance: claude
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/managed-by: Helm
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  selector:
    matchLabels:
      app.kubernetes.io/name: claude
      app.kubernetes.io/instance: claude
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: claude
        config.linkerd.io/opaque-ports: 8080,3000
        linkerd.io/inject: disabled
      labels:
        app.kubernetes.io/name: claude
        app.kubernetes.io/instance: claude
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      serviceAccountName: claude
      securityContext:
        fsGroup: 1000
        runAsGroup: 1000
        runAsNonRoot: true
        runAsUser: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        # Nginx reverse proxy for WebSocket support with long timeouts
        - name: nginx
          image: nginx:1.27-alpine
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 101
            runAsGroup: 101
            capabilities:
              drop:
                - ALL
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
          volumeMounts:
            - name: nginx-config
              mountPath: /etc/nginx/nginx.conf
              subPath: nginx.conf
            - name: nginx-tmp
              mountPath: /tmp
            - name: nginx-cache
              mountPath: /var/cache/nginx
            - name: nginx-log
              mountPath: /var/log/nginx
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              memory: 64Mi
        - name: claude
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
            readOnlyRootFilesystem: false
          image: "ghcr.io/jomcgi/homelab/charts/claude:main@sha256:5c9a6049857a4a51c4c8817efb7144886225fa4ca4561aeefe0900004b03eef8"
          imagePullPolicy: IfNotPresent
          ports:
            - name: backend
              containerPort: 3000
              protocol: TCP
            - name: ttyd
              containerPort: 7681
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: backend
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: backend
            initialDelaySeconds: 5
            periodSeconds: 2
            timeoutSeconds: 5
            failureThreshold: 3
          env:
            - name: HOME
              value: /home/user
            - name: PORT
              value: "3000"
            - name: GIT_USER_NAME
              value: "Joe McGinley"
            - name: GIT_USER_EMAIL
              value: "joe@jomcgi.dev"
            - name: GEMINI_MODEL
              value: "gemini-2.0-flash"
            - name: GITHUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: claude-secret
                  key: github_token
            - name: GOOGLE_API_KEY
              valueFrom:
                secretKeyRef:
                  name: claude-secret
                  key: google_api_key
                  optional: true
            - name: BUILDBUDDY_API_KEY
              valueFrom:
                secretKeyRef:
                  name: claude-secret
                  key: buildbuddy_api_key
                  optional: true
            # OpenCode config path (baked into image, not on PVC)
            - name: OPENCODE_CONFIG
              value: /etc/opencode/opencode.json
            - name: DEFAULT_WORKING_DIRECTORY
              value: "/repos/homelab"
            - name: DEFAULT_PERMISSION_MODE
              value: "bypassPermissions"
            # Repo sync configuration (replaces git-sync sidecar)
            - name: REPO_SYNC_URL
              value: "https://github.com/jomcgi/homelab"
            - name: REPO_SYNC_PATH
              value: /repos/homelab
            - name: REPO_SYNC_BRANCH
              value: "main"
            - name: REPO_SYNC_INTERVAL
              value: "60"
          resources:
            limits:
              memory: 12Gi
            requests:
              cpu: "1"
              memory: 12Gi
          volumeMounts:
            - name: home
              mountPath: /home/user
            # Repo sync uses a subdirectory on the home PVC for persistence
            - name: home
              mountPath: /repos/homelab
              subPath: repos/homelab
      volumes:
        - name: home
          persistentVolumeClaim:
            claimName: claude-home
        - name: nginx-config
          configMap:
            name: claude-nginx
        - name: nginx-tmp
          emptyDir: {}
        - name: nginx-cache
          emptyDir: {}
        - name: nginx-log
          emptyDir: {}
---
# Source: claude/templates/secret.yaml
apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: claude-secret
  labels:
    helm.sh/chart: claude-0.1.0
    app.kubernetes.io/name: claude
    app.kubernetes.io/instance: claude
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/managed-by: Helm
spec:
  itemPath: "vaults/k8s-homelab/items/claude.jomcgi.dev"
---
# Source: claude/templates/secret.yaml
apiVersion: onepassword.com/v1
kind: OnePasswordItem
type: kubernetes.io/dockerconfigjson
metadata:
  name: ghcr-pull-secret
  labels:
    helm.sh/chart: claude-0.1.0
    app.kubernetes.io/name: claude
    app.kubernetes.io/instance: claude
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/managed-by: Helm
spec:
  itemPath: "vaults/k8s-homelab/items/ghcr-read-permissions"
