
name: Deploy Obsidian

on:
  schedule:
    - cron: '0 0 * * *'
  push:
    branches:
      - main
    paths:
      - '.github/workflows/deploy-obsidian.yaml'
      - 'cluster/services/obsidian/**/**'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  deploy-obsidian:
    runs-on: self-hosted
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v3
      - name: Deploy Obsidian
        run:
          skaffold run -f cluster/services/obsidian/skaffold.yaml
        env:
          KUBECONFIG: /home/jomcgi/.kube/talos