name: Opentelemetry Collector

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/deploy-opentelemetry-collector.yaml'
      - 'cluster/opentelemetry-collector/**'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  deploy-opentelemetry-collector:
    runs-on: self-hosted
    
    steps:
      - uses: actions/checkout@v3
      - name: Deploy Opentelemetry Collector
        env:
          GH_ACTIONS_WEBHOOK_SECRET: ${{ secrets.GH_ACTIONS_WEBHOOK_SECRET }}
        run: |
          skaffold run -f cluster/opentelemetry-collector/skaffold.yaml