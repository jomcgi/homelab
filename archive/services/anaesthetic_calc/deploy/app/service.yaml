apiVersion: v1
kind: Service
metadata:
  name: anaesthetic-calc-service
spec:
  selector:
    app: anaesthetic-calc
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9090
      nodePort: 30234
  type: LoadBalancer