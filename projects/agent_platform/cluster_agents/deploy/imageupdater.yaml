apiVersion: argocd-image-updater.argoproj.io/v1alpha1
kind: ImageUpdater
metadata:
  name: cluster-agents
  namespace: argocd
spec:
  applicationRefs:
    - images:
        - alias: cluster-agents
          commonUpdateSettings:
            updateStrategy: digest
            forceUpdate: false
          imageName: ghcr.io/jomcgi/homelab/services/cluster-agents:main
          manifestTargets:
            helm:
              name: image.repository
              tag: image.tag
      namePattern: prod-cluster-agents
  namespace: argocd
  writeBackConfig:
    method: git:secret:argocd/argocd-image-updater-token
    gitConfig:
      repository: https://github.com/jomcgi/homelab.git
      branch: main
      writeBackTarget: helmvalues:values-prod.yaml
