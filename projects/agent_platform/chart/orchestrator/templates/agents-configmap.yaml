apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agent-orchestrator.fullname" . }}-agents
  labels:
    {{- include "agent-orchestrator.labels" . | nindent 4 }}
data:
  agents.json: {{ .Values.agentsConfig | toJson | quote }}
