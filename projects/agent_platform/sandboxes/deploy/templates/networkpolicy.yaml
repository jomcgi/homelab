apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-orchestrator-to-runner
  namespace: {{ .Release.Namespace }}
spec:
  podSelector:
    matchLabels:
      agents.x-k8s.io/sandbox-template: {{ .Values.sandboxTemplate.name }}
  policyTypes:
    - Ingress
  ingress:
    - from:
        {{- if .Values.orchestratorNamespace }}
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.orchestratorNamespace }}
        {{- else }}
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: agent-orchestrator
        {{- end }}
      ports:
        - protocol: TCP
          port: 8081
