{{- $hasRegistration := false }}
{{- range .Values.servers }}
{{- if and .registration .registration.enabled }}
{{- $hasRegistration = true }}
{{- end }}
{{- end }}
{{- if $hasRegistration }}
apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: {{ .Values.gateway.secret.name }}
  labels:
    {{- include "mcp-servers.labels" (dict "server" (dict "name" "gateway") "Chart" .Chart "Release" .Release) | nindent 4 }}
spec:
  itemPath: {{ .Values.gateway.secret.itemPath | quote }}
{{- end }}
