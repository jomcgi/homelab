apiVersion: v1
kind: Service
metadata:
  name: agent-sandbox-controller
  namespace: { { .Release.Namespace } }
  labels:
    app: agent-sandbox-controller
spec:
  selector:
    app: agent-sandbox-controller
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
