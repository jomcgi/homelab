apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mcp-oauth-proxy
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/jomcgi/homelab.git
    path: projects/agent-platform/mcp-oauth-proxy/deploy
    targetRevision: HEAD
    helm:
      releaseName: mcp-oauth-proxy
      valueFiles:
        - values.yaml
        - values-prod.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: mcp-gateway
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
