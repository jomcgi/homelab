# Production overrides for Context Forge MCP gateway
# Base values in charts/context-forge/values.yaml

mcp-stack:
  mcpContextForge:
    resources:
      requests:
        cpu: 50m
        memory: 1Gi
      limits:
        cpu: 200m
        memory: 1Gi
