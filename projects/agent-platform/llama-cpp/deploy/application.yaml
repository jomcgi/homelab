apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: llama-cpp
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/jomcgi/homelab.git
    path: projects/agent-platform/llama-cpp/deploy
    targetRevision: HEAD
    helm:
      releaseName: llama-cpp
      valueFiles:
        - values.yaml
        - values-prod.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: llama-cpp
  syncPolicy:
    managedNamespaceMetadata:
      labels:
        oci-model-cache.jomcgi.dev/enabled: "true"
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
