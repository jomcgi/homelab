apiVersion: onepassword.com/v1
kind: OnePasswordItem
metadata:
  name: {{ .Values.git.tokenSecret.name }}
  labels:
    {{- include "todo.labels" . | nindent 4 }}
spec:
  itemPath: {{ .Values.git.onePasswordItem | quote }}
