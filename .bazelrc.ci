# CI-specific configuration for self-hosted GitHub Actions runners
# This file is imported via try-import in user.bazelrc during CI builds
#
# Note: Heavy builds (build, test, push) now run on BuildBuddy Workflows with persistent VMs
# This configuration is only used for lightweight tasks (format checks) on self-hosted runners

common --config=ci
