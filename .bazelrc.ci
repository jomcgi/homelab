# CI-specific configuration for GitHub Actions runners
# This file is imported via try-import in user.bazelrc during CI builds

# Repository cache is pre-populated by init container from shared PVC
# Cache is copied to default writable location (~/.cache/bazel/repository_cache)
# This prevents download storms while allowing Bazel to write new dependencies

common --config=ci
