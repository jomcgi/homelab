apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namePrefix: homelab-

resources:
  - ../../overlays/base/bootstrap

configMapGenerator:
  - name: overlay-config
    literals:
      - overlay-name=homelab
    options:
      disableNameSuffixHash: true