actions:
  # Format check - runs formatters and gazelle, verifies no changes needed
  # Runs in parallel with Test action
  - name: "Format check"
    container_image: "ubuntu-24.04"
    resource_requests:
      disk: "30GB"  # Slightly larger for format tools
    triggers:
      push:
        branches:
          - "main"
      pull_request:
        branches:
          - "*"
    steps:
      - run: |
          # Skip if commit is from argocd-image-updater
          if [ "$(git log -1 --format='%an')" = "argocd-image-updater" ]; then
            echo "Skipping argocd-image-updater commit"
            exit 0
          fi
          # Run full format (includes formatters + gazelle)
          bazel run //tools/format:fast_format
          if ! git diff --exit-code; then
            echo "ERROR: Code not formatted or BUILD files out of sync."
            echo "Run 'format' locally to fix."
            echo ""
            echo "Changes detected:"
            git --no-pager diff
            exit 1
          fi

  # Test, Push, and Validate - sequential workflow
  # PR branches: Run tests only
  # Main branch: Run tests → Push images → Validate → Tag as "main"
  - name: "Test, Push, and Validate"
    container_image: "ubuntu-24.04"
    resource_requests:
      disk: "75GB"
    triggers:
      push:
        branches:
          - "main"
      pull_request:
        branches:
          - "*"
    steps:
      # Step 1: Run tests (both PR and main)
      - run: |
          # Skip if commit is from argocd-image-updater
          if [ "$(git log -1 --format='%an')" = "argocd-image-updater" ]; then
            echo "Skipping argocd-image-updater commit"
            exit 0
          fi
          # Exclude external tests (GHCR validation) - run in step 3
          bazel test //... --config=ci --deleted_packages=tools/python --test_tag_filters=-external

      # Step 2: Push images (main branch only)
      - run: |
          if [ "$(git log -1 --format='%an')" = "argocd-image-updater" ]; then
            exit 0
          fi

          # Only push images on main branch (not PRs)
          current_branch=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo "")
          if [ "$current_branch" != "main" ]; then
            echo "PR branch detected - skipping image push"
            exit 0
          fi

          echo "Main branch detected - pushing images"
          # GHCR_TOKEN set in BuildBuddy Settings → Secrets
          echo "$GHCR_TOKEN" | docker login ghcr.io -u jomcgi --password-stdin
          bazel run //images:push_all --config=ci

      # Step 3: Validate pushed images (main branch only)
      - run: |
          if [ "$(git log -1 --format='%an')" = "argocd-image-updater" ]; then
            exit 0
          fi

          # Only validate on main branch (after images are pushed)
          current_branch=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo "")
          if [ "$current_branch" != "main" ]; then
            echo "PR branch detected - skipping GHCR validation"
            exit 0
          fi

          echo "Main branch detected - validating pushed images"
          # GHCR_TOKEN set in BuildBuddy Settings → Secrets
          export GITHUB_TOKEN="$GHCR_TOKEN"
          export CI=true  # Enable timestamp tag discovery

          # Run GHCR validation tests (tagged "ghcr")
          # Discovers and validates timestamp-tagged images before tagging as "main"
          bazel test //... --config=ci --test_tag_filters=ghcr --test_env=GITHUB_TOKEN --test_env=CI

      # Step 4: Tag validated images as "main" (main branch only)
      - run: |
          # Skip if commit is from argocd-image-updater
          if [ "$(git log -1 --format='%an')" = "argocd-image-updater" ]; then
            exit 0
          fi

          # Only tag on main branch
          current_branch=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo "")
          if [ "$current_branch" != "main" ]; then
            echo "PR branch detected - skipping tag application"
            exit 0
          fi

          echo "Applying 'main' tag to validated images..."

          # GHCR_TOKEN set in BuildBuddy Settings → Secrets
          export GITHUB_TOKEN="$GHCR_TOKEN"

          # Get crane from Bazel (ensures consistent version)
          CRANE=$(bazel run --run_under=echo @rules_oci//oci:crane)

          # Authenticate to GHCR
          echo "$GHCR_TOKEN" | $CRANE auth login ghcr.io -u jomcgi --password-stdin

          # Tag all validated model images with "main"
          # These correspond to the GHCR validation tests that just passed
          for model in qwen2_5_0_5b_gguf qwen2_5_0_5b_st qwen3_30b_a3b_awq; do
            repo="ghcr.io/jomcgi/homelab/models/${model}"

            # List all tags and find the most recent timestamp tag
            # Timestamp format: YYYY.MM.DD.HH.MM.SS-shortsha
            latest_tag=$($CRANE ls "${repo}" 2>/dev/null | \
              grep -E '^[0-9]{4}\.[0-9]{2}\.[0-9]{2}\.' | \
              sort -r | head -1)

            if [ -z "$latest_tag" ]; then
              echo "WARNING: No timestamp tag found for ${model}, skipping..."
              continue
            fi

            echo "✓ Tagging ${repo}:${latest_tag} as 'main'"
            $CRANE tag "${repo}:${latest_tag}" main
          done

          echo "✓ All validated images tagged with 'main'"


  # Deploy CF Pages - runs in parallel with other actions, main branch only
  # TODO: Enable after CF_API_TOKEN is configured in BuildBuddy secrets
  # - name: "Deploy Pages"
  #   container_image: "ubuntu-24.04"
  #   resource_requests:
  #     disk: "30GB"
  #   triggers:
  #     push:
  #       branches:
  #         - "main"
  #   steps:
  #     - run: |
  #         # Skip if commit is from argocd-image-updater
  #         if [ "$(git log -1 --format='%an')" = "argocd-image-updater" ]; then
  #           echo "Skipping argocd-image-updater commit"
  #           exit 0
  #         fi
  #         # CF_API_TOKEN set in BuildBuddy Settings → Secrets
  #         export CLOUDFLARE_API_TOKEN="$CF_API_TOKEN"
  #         bazel run //websites:push_all_pages --config=ci
