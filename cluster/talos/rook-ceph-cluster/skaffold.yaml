apiVersion: skaffold/v4beta5
kind: Config
metadata:
  name: rook-ceph
deploy:
  helm:
    releases:
      - name: rook-ceph-cluster
        remoteChart: rook-release/rook-ceph-cluster
        version: v1.16.6
        namespace: rook-ceph
        createNamespace: true
        valuesFiles:
          - values.yaml