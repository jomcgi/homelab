apiVersion: skaffold/v4beta11
kind: Config
metadata:
  name: otel-collector

manifests:
  kustomize:
    paths:
    - ./cluster/otel-collector

deploy:
  kubectl:
    defaultNamespace: opentelemetry
    envVars:
      - GH_ACTIONS_WEBHOOK_SECRET