exporters:
  otlp:
    endpoint: "api.honeycomb.io:443"
    headers:
      x-honeycomb-team: "${HONEYCOMB_API_KEY}"

service:
  pipelines:
    traces:
      exporters:
        - otlp
    metrics:
      exporters:
        - otlp
    logs:
      exporters:
        - otlp