apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources: []

helmCharts:
  - name: cloudflare-tunnel
    releaseName: cluster-ingress
    version: 0.1.0
    repo: file://../../charts/cloudflare-tunnel
    namespace: cloudflare-tunnel
    valuesFile: ingress.yaml
