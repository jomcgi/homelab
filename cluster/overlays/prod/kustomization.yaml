apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namePrefix: prod-

resources:
  - ./cloudflare-tunnel
  - ./argocd-bootstrap

configMapGenerator:
  - name: overlay-config
    literals:
      - overlay-name=prod
    options:
      disableNameSuffixHash: true