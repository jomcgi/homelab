apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - nginx-public-status-deployment.yaml
  - nginx-public-status-service.yaml
configMapGenerator:
  - name: nginx-public-status-config
    files:
      - nginx-public-status-conf.conf
