apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: find-good-hikes-data
  namespace: find-good-hikes
  labels:
    app: find-good-hikes
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
  storageClassName: longhorn