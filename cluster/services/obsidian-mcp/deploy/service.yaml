apiVersion: v1
kind: Service
metadata:
  name: obsidian-mcp
  namespace: obsidian-mcp
  labels:
    app.kubernetes.io/name: obsidian-mcp
    app.kubernetes.io/component: server
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: mcp
    protocol: TCP
    name: mcp
  selector:
    app.kubernetes.io/name: obsidian-mcp
    app.kubernetes.io/component: server