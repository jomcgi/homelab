---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: cloudflaretunnels.tunnels.cloudflare.io
spec:
  group: tunnels.cloudflare.io
  names:
    kind: CloudflareTunnel
    listKind: CloudflareTunnelList
    plural: cloudflaretunnels
    singular: cloudflaretunnel
  scope: Namespaced
  versions:
    - additionalPrinterColumns:
        - jsonPath: .status.phase
          name: Phase
          type: string
        - jsonPath: .status.tunnelId
          name: TunnelID
          type: string
        - jsonPath: .status.active
          name: Active
          type: boolean
        - jsonPath: .metadata.creationTimestamp
          name: Age
          type: date
      name: v1
      schema:
        openAPIV3Schema:
          description: CloudflareTunnel is the Schema for the cloudflaretunnels API.
          properties:
            apiVersion:
              description: |-
                APIVersion defines the versioned schema of this representation of an object.
                Servers should convert recognized schemas to the latest internal value, and
                may reject unrecognized values.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
              type: string
            kind:
              description: |-
                Kind is a string value representing the REST resource this object represents.
                Servers may infer this from the endpoint the client submits requests to.
                Cannot be updated.
                In CamelCase.
                More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
              type: string
            metadata:
              type: object
            spec:
              description: CloudflareTunnelSpec defines the desired state of CloudflareTunnel.
              properties:
                accountId:
                  description: AccountID specifies the Cloudflare account ID
                  type: string
                configSource:
                  default: cloudflare
                  description: ConfigSource specifies the configuration source
                  type: string
                ingress:
                  description: Ingress specifies the ingress rules for the tunnel
                  items:
                    description: TunnelIngress defines ingress rules for the tunnel
                    properties:
                      hostname:
                        description:
                          Hostname specifies the hostname to route traffic
                          from
                        type: string
                      service:
                        description: Service specifies the target service URL
                        type: string
                    required:
                      - service
                    type: object
                  type: array
                name:
                  description: Name specifies the tunnel name in Cloudflare
                  type: string
              required:
                - accountId
                - name
              type: object
            status:
              description: CloudflareTunnelStatus defines the observed state of CloudflareTunnel.
              properties:
                active:
                  description: Active indicates if the tunnel has active connections
                  type: boolean
                conditions:
                  description: Standard Kubernetes conditions
                  items:
                    description:
                      Condition contains details for one aspect of the current
                      state of this API Resource.
                    properties:
                      lastTransitionTime:
                        description: |-
                          lastTransitionTime is the last time the condition transitioned from one status to another.
                          This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                        format: date-time
                        type: string
                      message:
                        description: |-
                          message is a human readable message indicating details about the transition.
                          This may be an empty string.
                        maxLength: 32768
                        type: string
                      observedGeneration:
                        description: |-
                          observedGeneration represents the .metadata.generation that the condition was set based upon.
                          For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                          with respect to the current state of the instance.
                        format: int64
                        minimum: 0
                        type: integer
                      reason:
                        description: |-
                          reason contains a programmatic identifier indicating the reason for the condition's last transition.
                          Producers of specific condition types may define expected values and meanings for this field,
                          and whether the values are considered a guaranteed API.
                          The value should be a CamelCase string.
                          This field may not be empty.
                        maxLength: 1024
                        minLength: 1
                        pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                        type: string
                      status:
                        description: status of the condition, one of True, False, Unknown.
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                        type: string
                      type:
                        description: type of condition in CamelCase or in foo.example.com/CamelCase.
                        maxLength: 316
                        pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                        type: string
                    required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                    type: object
                  type: array
                errorMessage:
                  description: ErrorMessage contains the error that caused the Failed state
                  type: string
                lastState:
                  description: LastState stores the state before transitioning to Failed
                  type: string
                observedGeneration:
                  description:
                    ObservedGeneration reflects the generation of the most
                    recently observed spec
                  format: int64
                  type: integer
                observedPhase:
                  description: ObservedPhase stores the phase value when it was unrecognized (for Unknown state)
                  type: string
                phase:
                  description: Phase is the current state machine phase
                  enum:
                    - Pending
                    - CreatingTunnel
                    - CreatingSecret
                    - ConfiguringIngress
                    - Ready
                    - Failed
                    - DeletingTunnel
                    - Deleted
                    - Unknown
                  type: string
                ready:
                  description: Ready indicates if the tunnel is ready for use
                  type: boolean
                retryCount:
                  description: RetryCount tracks retry attempts from Failed state
                  type: integer
                secretName:
                  description: SecretName is the name of the Secret containing the tunnel credentials
                  type: string
                tunnelId:
                  description: TunnelID is the Cloudflare tunnel ID
                  type: string
              required:
                - active
                - ready
              type: object
          type: object
      served: true
      storage: true
      subresources:
        status: {}
