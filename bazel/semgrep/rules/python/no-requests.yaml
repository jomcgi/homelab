rules:
  - id: no-requests
    languages: [python]
    severity: WARNING
    message: >-
      Use httpx instead of requests. httpx supports async/await for our
      FastAPI services.
    metadata:
      category: best-practices
    pattern-either:
      - pattern: import requests
      - pattern: from requests import $X
