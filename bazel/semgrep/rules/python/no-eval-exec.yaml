rules:
  - id: no-eval-exec
    languages: [python]
    severity: ERROR
    message: >-
      Avoid dynamic code execution, use ast.literal_eval() for data parsing.
    metadata:
      category: security
    pattern-either:
      - pattern: eval(...)
      - pattern: exec(...)
