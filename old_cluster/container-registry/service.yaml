apiVersion: v1
kind: Service
metadata:
  name: container-registry-service
  labels:
    app: container-registry
spec:
  selector:
    app: container-registry
  ports:
    - protocol: TCP
      port: 30500
      targetPort: 30500
      nodePort: 30500
  type: NodePort