# User-specific settings file for this repo
.aspect/bazelrc/user.bazelrc
user.bazelrc

# Temporary file created when RBE is disabled in CI
.bazelrc.remote.disabled

# Don't check in lockfile for now. It is unstable.
# https://github.com/bazelbuild/bazel/issues/19026
# https://github.com/bazelbuild/bazel/issues/19621
# https://github.com/bazelbuild/bazel/issues/19971
# https://github.com/bazelbuild/bazel/issues/20272
# https://github.com/bazelbuild/bazel/issues/20369
MODULE.bazel.lock

# Symlinks created by Bazel into the output tree
/bazel-*

# Local developer tools (from bootstrap.sh / crane export)
.tools/
# CI-local tool symlinks (from buildbuddy.yaml format check)
.ci-tools/


node_modules/
dist/
.wrangler/


*.venv/
__pycache__
*.sqlite
*.sqlite.db

.astro
.playwright-mcp
websites/hikes.jomcgi.dev/test-results/

# Rendered manifests - ArgoCD renders at deploy time, no need to commit
**/manifests/

# Local logs
logs/

# Environment files (keep .env.example templates)
.env
.env.local
.env.*.local

# Secrets and credentials
*.pem
*.key
*.p12
*.pfx
credentials.json
kubeconfig
*.kubeconfig
**/.claude/settings.local.json

# OS files
.DS_Store
Thumbs.db

# Editor files
.idea/
*.swp
.ruff_cache/
